<h1>All Mah Songs</h1>

<% @songs.each do |song| %>
  <li><%= link_to song.title, song %>
  <%= link_to "edit", edit_song_path(song) %>
  <%= link_to "delete", song_path(song), method: :delete %></li>
<% end  %>

<h3><%= link_to "New Song", new_song_path %></h3>
