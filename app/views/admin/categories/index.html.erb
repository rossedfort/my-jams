<div class="container center">
  <h1>All Mah Categories</h1>
  <% @categories.each do |category| %>
    <ul>
      <li><%= category.name %></li>
    </ul>
  <% end %>
  <%= link_to "Create Category", new_admin_category_path %>
</div>
