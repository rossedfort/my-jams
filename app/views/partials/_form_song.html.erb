<%= form_for(@song) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :artists %>
  <div class="input-field browser-default">
      <%= f.collection_select :artist_id, Artist.all, :id, :name, {}, {class: "browser-default"} %>
  </div>
  <%= f.submit %>
<% end  %>
