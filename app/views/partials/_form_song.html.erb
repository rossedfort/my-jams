<%= form_for(@song) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label "Artist" %>
  <%= f.collection_select :artist_id, Artist.all, :id, :name %>
  <%= f.submit %>
<% end  %>
